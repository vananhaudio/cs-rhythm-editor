---
name: project_journey_os
description: Tầm nhìn Journey Operating System cho /admin/schedule — lịch thật + Mira điều hành đào tạo cả năm
metadata: 
  node_type: memory
  type: project
  originSessionId: c93c73ef-05d5-45d2-87f9-c67f31261321
---

Thầy đưa spec lớn (gợi ý ChatGPT, 2026-07-02): biến `/admin/schedule` thành **hệ điều hành hành trình học viên + tuyển sinh + mở lớp 1 năm**, không chỉ là lịch. Spec đầy đủ: **docs/JOURNEY-OS-SPEC.md** (đã push GitHub). Mira ĐỀ XUẤT, thầy DUYỆT.

4 tầng: lịch thật · tiến trình lớp · bản đồ hành trình · Mira đề xuất (xếp/gộp/mở lớp, ưu đãi). 5 khu: Dashboard · Calendar thật · Class Timeline · Journey Map · Mira Planner.

**Điểm mấu chốt cho việc build:** ĐÃ CÓ nền (đừng tạo bảng trùng) — `edu_courses` (=courses, có mã năng lực + hanhtrinh.ts prereq), `class_schedule` (=classes, đang lưu lịch dạng TEXT), `edu_enrollments`, nhóm Zalo ≡ mã lớp + trigger cấp khoá, Mira full-context. CẦN MỚI: dates thật + tự sinh `class_sessions` (từng buổi), status enum lớp/buổi, `class_demands` (nhu cầu chờ), `offer_campaigns` (ưu đãi), `mira_recommendations`; UI Mira Planner + rules (R1-R5).

**MVP 4 GĐ:** (1) lịch thật + tự sinh buổi + tiến trình + tính ngày kết thúc [NỀN]; (2) bản đồ hành trình; (3) nhu cầu + lớp nháp; (4) ưu đãi + Mira Planner. Đây là build NHIỀU PHIÊN. Liên quan [[project_hanhtrinh2027_boluat]], [[project_mira_assistant]], [[project_admin_ai_assistant]], [[project_cohort_hanhtrinh]].

**GĐ4 ĐÃ BUILD & LIVE (2026-07-02) — HOÀN TẤT MVP 4 GĐ:** Ưu đãi + Mira Planner. SQL `db/journey_os_stage4.sql` (ĐÃ chạy) — bảng MỚI `offer_campaigns` (name, course_code/package_code, original/offer_price text, start/end_at date, quota/used_quota, target_rules, status draft/active/paused/expired; RLS thầy-only → self_managed). Tab '🎁 Ưu đãi' (`OffersBoard.tsx`): CRUD, trạng thái hiệu lực tự tính theo ngày+quota. Tab '🧭 Mira' (`MiraPlanner.tsx`): đề xuất bằng RULE thuần frontend (thầy chốt KHÔNG dùng AI/Edge Function) — R1 lớp còn 1-2 buổi→khoá tiếp (dùng `nextCourses()` mới trong hanhtrinh.ts, đảo PREREQ), R2 đủ 4 người/khoá→mở lớp, R3 ≥3 buổi/ngày→quá tải, R4 lớp DH2+/TN2+ sắp xong→mời HT2027+ưu đãi, R5 lớp nháp gần đủ→đẩy TT; mỗi thẻ có nút hành động + ẩn (dismiss client, chưa lưu). KHÔNG tạo bảng mira_recommendations (tính trực tiếp). Commit `32e2719`. **Lịch lớp giờ 7 tab: Danh sách·Lịch tuần·Bản đồ·Nhu cầu·Ưu đãi·Mira·Chỉ số.** Hướng mở rộng sau: nút thu nhu cầu phía HV (app), nối admin-ai diễn giải NN, siết weekday nhiều buổi/tuần, đánh bóng UI 7 tab.

**MỞ RỘNG ĐÃ LÀM — Nối lịch thật vào trang tuyển sinh công khai (2026-07-02):** `ClassLandingPage.tsx` (class.vananhaudio.com//class) trước chỉ đọc field TEXT (schedule/start_text) + phân nhóm theo cột `section` thủ công. GIỜ đọc thêm start_date/end_date/status → `bucketOf()`: TỰ ẨN status ∈ {draft,cancelled,merged,completed,paused} (không lộ lớp nháp!) + quá end_date; tự phân 'sắp khai giảng'/'đang học' theo ngày+status; lớp CŨ chưa có start_date → fallback theo section (không phá). Nhãn ngày động 'Khai giảng dd/mm · còn X ngày' / 'Đang học · kết thúc dd/mm'. RLS: anon chỉ đọc class_schedule, KHÔNG đọc class_sessions → đếm ngược suy từ class_schedule fields, không từ sessions. **status = công tắc hiển thị công khai từ nay.** Verify dev OK (3 lớp đếm ngược đúng). Commit `7b48eae`.

**MỞ RỘNG ĐÃ LÀM — Lưu & duyệt đề xuất Mira bền (2026-07-02):** SQL `db/journey_os_mira_recs.sql` (CẦN chạy) — bảng `mira_recommendations` lưu trạng thái theo `rec_key` ỔN ĐỊNH (R1:<classId>, R2:<code>, R3:<day>, R4/R5:<classId>), trigger updated_at, RLS thầy-only → self_managed. MiraPlanner: mỗi rec có key bền; nút ✓Duyệt/Đã xong/Ẩn (upsert), 2 tab con Đang chờ / Đã xử lý + badge + ↩ đưa lại chờ. Rule vẫn tính client mỗi lần, bảng chỉ giữ TRẠNG THÁI. Commit `2324209`. **Tổng bảng Journey OS cần thêm vào self_managed nếu chạy lại rls_setup: class_sessions, class_demands, offer_campaigns, mira_recommendations (+ giữ class_schedule).**

**GĐ3 ĐÃ BUILD & LIVE (2026-07-02):** Nhu cầu mở lớp + lớp nháp. SQL `db/journey_os_stage3.sql` (ĐÃ chạy) — bảng MỚI `class_demands` (student_id nullable + student_name nhập nhanh, course_code=mã năng lực, preferred_days/times, priority, source 'admin', status waiting/planned/enrolled/cancelled; RLS thầy-only → thêm vào self_managed nếu chạy lại rls_setup). NGUỒN NHU CẦU = THẦY NHẬP TAY ở /admin (thầy chốt; nút phía học viên để sau). Tab '📥 Nhu cầu' (`src/journey/DemandsBoard.tsx`): form ghi + gom theo mã năng lực; **rule R2** ngưỡng=4 người/khoá → badge 'Đủ mở lớp' + nút 'Tạo lớp nháp' (`createDraftFor` mở form lớp prefill khoá chính + status draft). Lớp nháp hiện nét đứt/nền nhạt trên Calendar. Commit `2d3ff94`. CÒN LẠI: GĐ4 Ưu đãi (offer_campaigns) + Mira Planner (mira_recommendations, rule R1/R3/R4/R5, nối admin-ai).

**GĐ2 ĐÃ BUILD & LIVE (2026-07-02):** Bản đồ hành trình — tab '🗺 Bản đồ' trong Lịch lớp (`src/journey/JourneyMap.tsx`). Vẽ 5 nhánh từ `hanhtrinh.ts` (Nhập môn · Đệm hát DH1→DH2→DH3→DHNC · Tỉa nốt TN1→TN2→TN3 · Nhạc lý NL1→NL2→NL3 · Solo) nối bằng →. Mỗi mốc = 1 mã năng lực: node đậm nếu `edu_courses` có code đó, mờ nếu chưa dựng. GẮN LỚP THẬT vào mốc qua `main_course_id`→code: hiện mã lớp + buổi X/8 + chấm màu status. THUẦN FRONTEND, KHÔNG SQL mới (student_journeys suy từ dữ liệu, không tạo bảng). Commit `7129d10`.

**GĐ1 ĐÃ BUILD & LIVE (2026-07-02):** SQL `db/journey_os_stage1.sql` (ĐÃ chạy) — nâng `class_schedule` (start_date/weekday 0=CN..6=T7/start_time/duration_minutes/total_sessions/end_date/status enum 11 trạng thái/max-min_students, GIỮ cột text cũ cho trang tuyển sinh) + bảng MỚI `class_sessions` (từng buổi: session_number/start_at/end_at/status 6 trạng thái; RLS thầy-only, anon KHÔNG đọc → nhớ thêm 'class_sessions' vào self_managed nếu chạy lại rls_setup.sql). Sinh buổi Ở CLIENT khi lưu lớp (`src/journey/sessions.ts`: generateSessions cách 7 ngày/1 buổi/tuần, giữ buổi đã completed khi sửa lịch). UI: `ScheduleManager` có 3 tab — Danh sách (badge buổi X/8 + chấm status) · **Lịch tuần** (`src/journey/CalendarWeek.tsx`, lưới T2-CN, click buổi→popup đánh dấu đã dạy/huỷ/dời/bù/nghỉ) · **Chỉ số** (`src/journey/ScheduleDashboard.tsx`). Giả định 1 buổi/tuần (weekday là int, chưa hỗ trợ nhiều buổi/tuần). CÒN LẠI: GĐ2 bản đồ hành trình, GĐ3 nhu cầu+lớp nháp, GĐ4 ưu đãi+Mira Planner.
