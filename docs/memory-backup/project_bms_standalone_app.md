---
name: bms-standalone-ios-app
description: "Beat my Songs (BMS) đang được publish thành APP iOS RIÊNG — VIẾT NATIVE SwiftUI (project ngoài repo), sẽ tích hợp lại vào TVA sau"
metadata: 
  node_type: memory
  type: project
  originSessionId: 26274173-3421-4b94-ad92-aab2ae5297a9
---

**Beat my Songs (BMS)** = tên + logo cho tính năng Song Builder (`src/SongBuilderPage.tsx`, route `/song-builder`). Văn Anh quyết định publish BMS thành **app iOS RIÊNG**, tách khỏi app LMS "TVA Guitar".

**HƯỚNG HIỆN TẠI (chốt 2026-06-04): VIẾT LẠI NATIVE SwiftUI** — Văn Anh bác cách vỏ Capacitor, tự tạo project SwiftUI trắng và muốn viết native (biết là "rất lâu").
- Project native: `/Users/vananhaudio/App/song builer/BMS/` (có git riêng, NGOÀI repo cs-rhythm-editor).
- pbxproj objectVersion 77 → dùng **PBXFileSystemSynchronizedRootGroup**: thả file .swift vào thư mục `BMS/BMS/` là tự vào build, KHÔNG cần sửa pbxproj.
- Bundle id `com.vananhaudio.BMS` · Display Name "Beat my Songs" (`INFOPLIST_KEY_CFBundleDisplayName`, đã set cả Debug+Release) · Team `S6ASX8GP62` · deployment target iOS 16.0.
- Icon BMS trắng 1024px không-alpha đã gắn (`AppIcon-1024.png`).
- ⚠️ **iOS KHÔNG có SDK YouTube native** → phát video vẫn phải nhúng YouTube IFrame API trong **WKWebView** (`YouTubePlayer.swift`). "Native" chỉ được UI/logic/metronome, không được video native.
- ⚠️ **Lỗi YouTube 152/153 trên device (2026-06-04):** WKWebView nạp HTML kiểu chèn (`loadHTMLString`/`loadSimulatedRequest`) KHÔNG gửi HTTP Referer thật → YouTube chặn nhúng MỌI video (mã 152/153 "player configuration error"). Sửa referer meta + nocookie KHÔNG đủ. **GIẢI PHÁP đang dùng:** `LocalServer.swift` = HTTP server tí hon (Network framework `NWListener`), CHỈ bind loopback `127.0.0.1` (port any) nên không xin quyền Local Network; `YouTubePlayerView` nạp player qua `http://127.0.0.1:<port>/` THẬT → iframe gửi Referer hợp lệ → hết lỗi. `playerVars.origin = window.location.origin`. Cần ATS `NSAllowsLocalNetworking=true`: thêm `Info.plist` ở **SRCROOT** (NGOÀI thư mục đồng bộ `BMS/BMS/`, nếu để trong sẽ lỗi "Multiple commands produce Info.plist") + set `INFOPLIST_FILE = Info.plist` cả 2 config; GIỮ `GENERATE_INFOPLIST_FILE=YES` → Xcode trộn các `INFOPLIST_KEY_*` vào plist này (đã verify CFBundleDisplayName/ITSAppUsesNonExemptEncryption vẫn còn).
- Build verify: `xcodebuild -scheme BMS -project BMS.xcodeproj -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO`. Typecheck nhanh file thuần: `swiftc -typecheck *.swift`.
- Đã làm: port logic thuần (`TempoFit.swift` ← tempoFit.ts, `SongMapping.swift` ← songBuilder.ts), `YouTubePlayer.swift` (WKWebView bridge: load/play/pause/seek + đọc currentTime ~100ms qua message handler "bms"). Kiến trúc UI: `Theme.swift` (palette C tối + BmsMark + BmsCard + BmsButtonStyle), `SongBuilderModel.swift` (ObservableObject = nguồn sự thật: step/videoId/lyrics/fit/anchors + words/mapping dẫn xuất + canNext/goNext), `YouTubeSearch.swift` (YouTube Data API v3, YT_API_KEY), `ContentView.swift` = shell (header logo + dot 6 bước + player + nav), `LyricBlock.swift` (chip lời + FlowLayout).
- **CẢ 6 BƯỚC UI ĐÃ DỰNG NATIVE (2026-06-04), BUILD SUCCEEDED:** `StepSetupView` (tìm/dán link + tên + lời), `StepTempoView` (TAP→BPM), `StepAnchorView` (gắn mốc + nội suy + picker sheet), `StepDownbeatView` (chọn Đều/2-3-4-6 + chỉ định phách mạnh), `StepPreviewView` (nghe thử + highlight từ đang hát + metronome), `StepExportView` (màn KHÓA chờ schema v2.1). Metronome: `Metronome.swift` (AVAudioEngine, click mạnh 1650Hz/nhẹ 920Hz, .mixWithOthers để mix tiếng video; driven bởi `.onReceive(player.$currentTime)`).
- **Lưu/khôi phục draft ĐÃ XONG (2026-06-04, BUILD SUCCEEDED):** `DraftStore.swift` (port songDraftStorage.ts → JSON file `bms-drafts-v1.json` ở Documents, currentId ở UserDefaults; save/load/list/delete/latest/progress/hasContent/relTime). `SongDraft` Codable; đã thêm `Codable` cho `TempoFit`/`TapAssign`/`Anchor` (đổi `let source` → `var source` để Codable). Model: `init()` tự nạp currentId + autosave debounce 1s qua `objectWillChange`; có `snapshot()/apply()/newDraft()`. `DraftsSheet.swift` (nút 🗂 ở header: list bài, tiếp tục/xoá/➕ bài mới). Lưu ý: tapTimes KHÔNG persist (chỉ lưu fit — giống web).
- **Polish native (2026-06-04, BUILD SUCCEEDED cả simulator Debug lẫn device Release):** `Haptics.swift` (Taptic: tap nhẹ khi TAP tempo + phách mạnh metronome, medium khi gắn mốc, success khi gán từ/xác nhận phách mạnh). Splash thương hiệu trong `ContentView` (BmsMark 76 + slogan, fade sau 0.8s). pbxproj: thêm `INFOPLIST_KEY_ITSAppUsesNonExemptEncryption = NO` (ghi đúng boolean `false` vào Info.plist → App Store Connect khỏi hỏi mã hoá). KHÔNG dùng mic/camera nên không cần usage-description.
- Còn lại: bước Xuất thật (chờ chốt schema v2.1 — xem [[song-builder-v1]]); icon vẫn là BMS trắng tạm (đủ điều kiện nộp nhưng nên thay icon đẹp hơn sau). Lưu ý kỹ thuật: biểu thức Text nối nhiều `+` dễ làm Swift "unable to type-check in reasonable time" → tách ra computed `var ...: Text`. ObservableObject cần `import Combine` (kể cả file AVFoundation). Bất kỳ build setting `INFOPLIST_KEY_<Tên>` nào cũng được Xcode bơm thẳng vào Info.plist sinh tự động.

- **ĐÃ NỘP DUYỆT APP STORE CÔNG KHAI (2026-06-04, 23:09):** App "Beat my Songs" (App Store Connect id `6776634840`, version 1.0 build 2) đã **Submit for Review → trạng thái Waiting for Review**. Submission ID `e4235f67-66df-4253-9546-27b743e201e3`. Nội dung đã khai: Category **Music + Education**; Subtitle VI "Luyện nhịp phách guitar" / EN "Guitar rhythm & beat trainer"; Keywords + Support URL `https://timming.vananhaudio.com`; Privacy URL `https://timming.vananhaudio.com/privacy` (trang tĩnh ở web repo `public/privacy/index.html`, khai cả locale EN lẫn VI); Description (5 bước) EN+VI; Age 4+; Free; Content Rights = "Yes, third-party content" + xác nhận có quyền (vì nhúng YouTube); Encryption miễn (ITSAppUsesNonExemptEncryption=NO). **DSA/EU Trader Status** đã khai Trader (mục Business → Digital Services Act = Active, 27 nước EU; công ty VAN ANH AUDIO COMPANY LIMITED, địa chỉ Hà Nội). App khai `TARGETED_DEVICE_FAMILY = "1,2"` (iPhone+iPad) nên App Store BẮT ảnh iPad 13" → đã tạo ảnh iPad 2064×2752 bằng `sips` (đặt ảnh iPhone 1290×2796 giữa nền tối #0B0B0F, không kéo méo) lưu ở `~/Downloads/bms-ipad/`. **Rủi ro chính khi duyệt:** Guideline 4.2 (minimum functionality) / 5.2.3 (app xoay quanh YouTube) → Review Notes đã nhấn tính năng native on-device (metronome AVAudioEngine, gắn mốc, nội suy, tô sáng lời theo nhịp). Nếu bị Rejected → trả lời ở Resolution Center giải thích phần native. Apple gửi email mỗi lần đổi trạng thái về `vananhaudio@gmail.com`.

- **ĐÃ PHÁT HÀNH CÔNG KHAI + đang làm bản 1.1 (2026-06-09):** App "Beat my Songs" đã được Apple duyệt và **lên App Store** (bản 1.0 build 2; vụ Guideline 2.1(b) "Information Needed" đã xử lý bằng cách Reply giải thích app free hoàn toàn + bấm Resubmit, KHÔNG cần build mới). **Bản cập nhật 1.1 đang dựng:** thêm **màn Luyện tập (Player)** `PracticeView.swift` = mở thẳng 1 SongDraft ĐÃ LƯU, phát video + metronome + tô sáng lời theo nhịp (tái dùng logic StepPreviewView nhưng nguồn là draft cố định, read-only; words/mapping dẫn xuất qua splitWords/computeMapping; metronome tick từ draft.fit/timeSignature/groupBeats/downbeatPosition). `DraftsSheet.swift` sửa: **chạm bài → mở PracticeView** (.fullScreenCover), thêm nút ✎ Sửa (nạp vào builder), giữ 🗑. **Kiến trúc chốt: `SongDraft` = nguồn cung dữ liệu cho Player** (Builder GHI qua snapshot(), Player ĐỌC qua DraftStore.load(); cùng cấu trúc videoId+lyricsText+fit+anchors+timeSignature/groupBeats/downbeatPosition). Đã bump `MARKETING_VERSION 1.0→1.1` + `CURRENT_PROJECT_VERSION 3→5` (cả Debug+Release), BUILD SUCCEEDED (simulator + Release/device validate-for-store). Việc còn lại của Văn Anh: Archive→Upload build 5 trong Xcode → tạo version 1.1 trên ASC + What's New + chọn build 5 → Submit. (Lưu ý: 1.0 đã release nên KHÔNG sửa đè được, bắt buộc tạo version mới.) Hướng mở rộng Player đã bàn (chưa làm): tốc độ chậm 0.5×/0.75× (cần setPlaybackRate vào YouTubePlayer), count-in, lặp đoạn A-B, chế độ chỉ-metronome; và xa hơn là đồng bộ kho bài lên Supabase (Thầy soạn→HV tải).

- **BUILD 5 / v1.1 ĐÃ UPLOAD LÊN ASC (2026-06-09):** Chốt thêm 2 nhóm tính năng vào build 5 rồi Archive (`xcodebuild ... -archivePath /tmp/BMS-build5.xcarchive archive` → ARCHIVE SUCCEEDED) + Văn Anh Upload qua Organizer XONG. Gồm:
  1. **Xuất/Nhập file `.bms`** (local-first, đúng triết lý Văn Anh: người dùng tự tạo→tự lưu→**⤴ gửi Thầy**; Thầy **⤵ nhập** bài hay vào thư viện riêng, KHÔNG cần server). `DraftStore.swift` thêm `exportData()`/`exportFile()` (ghi JSON đẹp ra temp `<tên>.bms`) + `importFile()` (đọc .bms, **cấp id mới** để không đè bài cũ rồi save). `DraftsSheet.swift`: nút **⤵ Nhập bài** cạnh ➕ Bài mới (`.fileImporter`, allowed `.bmsSong/.json/.data`), nút **⤴** mỗi dòng bung share sheet (`ActivityView` bọc `UIActivityViewController`), `extension UTType.bmsSong` (`UTType(filenameExtension:"bms")`), alert khi file hỏng. ShareItem wrapper cho `.sheet(item:)`.
  2. **Lời KARAOKE tự cuộn** (`KaraokeLyrics.swift` MỚI = `KaraokeLyricsView` + `CenteredFlow` Layout căn-giữa-mỗi-hàng). Dòng đang hát tự `proxy.scrollTo(anchor:.center)` (ScrollViewReader, animation 0.45s) trôi về giữa màn; từ đang hát sáng vàng to 27pt + shadow; dòng theo khoảng cách activeLine mờ dần (1/0.5/0.32/0.2); mask gradient mép trên/dưới. `PracticeView.swift` sửa: **bỏ ô bigWord riêng** (gộp vào karaoke), **bỏ ScrollView cả trang** (đúng nguyên tắc Flow Player = không cuộn trang) → video+controls cố định trên, `lyricsCard` (karaoke) `maxHeight:.infinity` chiếm hết phần còn lại + tự cuộn bên trong; fallback khi !hasGrid hiện lời tĩnh.
  - **CÒN LẠI:** trên ASC, chờ build 5 "Processing" xong → tạo version **iOS 1.1** → What's New (EN, primary locale English) → chọn build 5 → Add for Review → Submit.

- **iOS 1.1 build 5 ĐÃ SUBMIT (2026-06-09):** tạo version 1.1, chọn build 5, Submit → "Waiting for Review" (Submission ID 72d66a90-b787-4516-8a2b-7cc0bab7657a). Xong phần iOS.

- **ANDROID qua Google Play (Capacitor) — đã upload, đang chờ duyệt (tính tới 2026-06-15):** Beat my Songs cũng publish Android. **Dự án Capacitor có sẵn ở `~/Desktop/beat-my-songs/`** (KHÁC project SwiftUI native): appId `com.vananhaudio.beatmysongs`, `capacitor.config.json` server.url=`https://timming.vananhaudio.com/song-builder?standalone=1` (bọc web standalone, tái dùng code web — KHÁC bản iOS là native SwiftUI). Android build: AGP 8.13.2/Gradle 8.13 **cần JDK 17** (máy mặc định Java 11 → dùng JBR trong `/Applications/Android Studio.app/Contents/jbr/Contents/Home`); SDK ở `~/Library/Android/sdk`; keystore `~/Desktop/bms-release.jks` (đã thêm signingConfig đọc từ `android/keystore.properties` vào `app/build.gradle`, file keystore.properties tạo sẵn nhưng CHƯA điền mật khẩu). Văn Anh đã tự build & upload `.aab` versionCode 1 (1.0) lên cả internal/closed(Alpha)/production. **Production "Đang xem xét" >6 ngày** — đã kiểm tra "Tổng quan về việc xuất bản": KHÔNG có lỗi/Action required, "Chế độ xuất bản có quản lý đang TẮT" (duyệt xong tự lên Play). 2 lỗi ở bản nháp "Thử nghiệm mở" là VÔ CAN (track trống, tách biệt). Tài khoản Play là **Tổ chức/Công ty** → KHÔNG dính luật test 12 người/14 ngày. Kết luận: chỉ là Google duyệt lần đầu chậm, chờ tiếp; tránh bấm "Xoá các thay đổi" (huỷ lượt duyệt).

- **TÍNH NĂNG HỢP ÂM cho BMS native (2026-06-15, BUILD SUCCEEDED, CHƯA bump version/chưa nộp):** Thêm hợp âm vào app. Gồm: (1) **Dữ liệu**: `struct SongChord {wordIndex, name}` + field `chords: [SongChord]?` (optional, tương thích bài cũ) trong SongDraft; SongBuilderModel thêm `@Published chords` + `setChord/clearChord/chordName(at:)/resetChords`, snapshot/apply/newDraft cập nhật. (2) **Bước "Hợp âm" mới** (`StepChordView.swift`): chèn vào STEPS giữa "Nhịp" và "Nghe thử" → `["Chuẩn bị","Phách","Gắn mốc","Nhịp","Hợp âm","Nghe thử","Xuất"]`; ContentView case 4=Chord, 5=Preview, 6=Export; navBar đổi `step==4` → `step==STEPS.count-2`. Chạm từ → `ChordPickerSheet` (lưới hợp âm kèm sơ đồ bấm) gán/xoá. (3) **Thư viện** `ChordLibrary.swift`: 21 hợp âm mở thông dụng (C,G,D,A,E,Am,Em,Dm,F,Fmaj7,Bm,F#m,B7,A7,E7,D7,G7,C7,Cadd9,Dsus4,Asus2) + `ChordDiagramView` (Canvas vẽ sơ đồ bấm ngón). (4) **Player**: `KaraokeLyricsView` nhận `chords`, hiện nhãn hợp âm (accent) trên từ; `PracticeView` thêm `chordStrip` (hợp âm hiện tại to + sơ đồ bấm) tính theo từ đang hát (hợp âm gần nhất wordIndex ≤ active). CÒN LẠI: chạy thử simulator xem trực quan; nếu OK thì bump build 6/version 1.2 nộp tiếp.

- **TỰ NHẬN HỢP ÂM khi dán lời (2026-06-15, BUILD SUCCEEDED):** `LyricsChordParser.swift` (thuần) — `parseLyricsWithChords(raw) -> (lyrics sạch, [SongChord])` + `hasChordMarkup` + `isChordToken` (regex root [A-G][#b]? + maj/m/sus/add/dim/aug/số + /bass). Hỗ trợ 2 định dạng: (1) ngoặc ChordPro `[Em]Có khi nào [C]em` (gắn hợp âm vào từ NGAY SAU ngoặc); (2) dòng toàn hợp âm NẰM TRÊN dòng lời, map theo CỘT ký tự → từ. Map theo wordIndex khớp splitWords (đếm token theo whitespace, index liên tục, bỏ qua line bị drop). Mỗi từ giữ 1 hợp âm (dedupe ưu tiên đầu). Tích hợp `StepSetupView`: ô lời bind `@State rawLyrics` (đồng bộ từ model.lyricsText khi onAppear), `.onChange` → nếu có markup thì tự parse: set model.lyricsText=lời sạch + model.chords, hiện rawLyrics=lời sạch (bỏ markup), báo "🎸 Đã tự nhận N hợp âm". Không markup → chỉ cập nhật lyrics, KHÔNG đụng chords thủ công.

- **TINH CHỈNH HỢP ÂM (2026-06-15, BUILD SUCCEEDED):** (1) **Sửa hợp âm ngay trong Player**: PracticeView đổi `chords` thành `@State` + custom `init(draft:)`; dải hợp âm (chordStrip) thành Button có nhãn "✎ sửa", chạm → ChordPickerSheet, áp vào `editTargetWordIndex` (từ của hợp âm đang chơi, hoặc từ đang hát) → cập nhật state + `DraftStore.save` (lưu vĩnh viễn). (2) **Hợp âm ở màn Nghe thử**: LyricBlock thêm param `chords` (hiện nhãn accent trên từ, dùng chung — anchor step không truyền nên không đổi); StepPreviewView hiện "Hợp âm: X" theo từ đang hát + truyền chords vào LyricBlock. CHƯA bump version/nộp — Văn Anh muốn TestFlight sau khi tinh chỉnh xong.

- **iOS 1.2 build 6 ĐÃ ARCHIVE + UPLOAD (2026-06-15):** Bump `MARKETING_VERSION 1.1→1.2` + `CURRENT_PROJECT_VERSION 5→6` (cả Debug+Release trong BMS.xcodeproj của app NATIVE `com.vananhaudio.BMS`). Archive `/tmp/BMS-build6.xcarchive` → ARCHIVE SUCCEEDED → Văn Anh Upload qua Organizer XONG ("Uploaded to Apple", build 6, com.vananhaudio.BMS). Gói đủ: karaoke cuộn + Player + Xuất/Nhập .bms + toàn bộ hợp âm (gán thủ công/tự nhận khi dán lời/sơ đồ bấm/hợp âm trôi trên lời/sửa trong Player/hợp âm ở Nghe thử). ⚠️ LƯU Ý 2 PROJECT KHÁC NHAU: native SwiftUI = `~/App/song builer/BMS/BMS.xcodeproj` bundle `com.vananhaudio.BMS` (app đã lên App Store, CHỨA hợp âm); Capacitor = `~/Desktop/beat-my-songs/` bundle `com.vananhaudio.beatmysongs` (chỉ để bọc Android cho Google Play) — ĐỪNG nhầm khi build/archive. KẾ HOẠCH: nộp 1.2 build 6 lên App Store (tạo version 1.2 trên ASC, What's New EN gồm Player+karaoke+chia sẻ .bms+hợp âm, chọn build 6, Submit); 1.1(5) ĐÃ được Apple phê duyệt/phát hành rồi (không vướng) → cứ tạo version 1.2 bình thường, What's New chỉ cần nói về HỢP ÂM (1.1 đã có Player/karaoke/chia sẻ). Mai cải tiến tiếp.

- **iOS 1.2 (HỢP ÂM) ĐÃ ĐƯỢC APPLE DUYỆT (2026-06-14, Submission 88f87d0b): "eligible for distribution"** → app `id6776634840` https://apps.apple.com/app/beat-my-songs/id6776634840. Tùy chế độ phát hành (auto thì tự lên ≤24h; manual thì bấm "Release this version").

- **PORT BMS LÊN WEB/DESKTOP (bắt đầu 2026-06-15):** Văn Anh muốn BMS chạy trên máy tính. Chọn hướng **PORT tính năng mới (iOS native) sang bản WEB** `timming.vananhaudio.com/song-builder` (React, file `src/SongBuilderPage.tsx` ~1090 dòng trong repo cs-rhythm-editor) để chạy MỌI máy tính (Win+Mac). LƯU Ý web là bản CŨ tách khỏi native — trước port chỉ có trình dựng bài, KHÔNG có hợp âm/Player/karaoke/.bms. Làm theo 3 phase. **PHASE 1 XONG + verify trình duyệt OK (tsc sạch):** thêm `SongChord` + field `chords?` (songBuilder.ts, songDraftStorage.ts); `src/logic/chordLibrary.ts` (21 hợp âm) + `src/ChordDiagram.tsx` (SVG sơ đồ bấm); `src/logic/lyricsChordParser.ts` (parseLyricsWithChords/hasChordMarkup/isChordToken — port từ Swift, 2 định dạng [Em] & dòng-trên); tích hợp SongBuilderPage: STEPS thêm 'Hợp âm' (giờ 6 bước: Chuẩn bị/Phách/Gắn mốc/Nhịp/Hợp âm/Nghe thử), state chords + autosave, bước StepChord + ChordPickerModal, tự nhận hợp âm khi dán lời (handleLyrics), LyricBlock+StepPreview hiện hợp âm. **CÒN: Phase 2** (Player luyện tập web: karaoke cuộn + dải hợp âm + "Bài của tôi"), **Phase 3** (Xuất/Nhập .bms + responsive desktop — hiện layout vẫn mobile 1 cột). Deploy: commit+push main → Netlify. **PHASE 1 ĐÃ COMMIT+PUSH** (commit 5aea180, live timming.vananhaudio.com/song-builder; Android Capacitor cũng tự có vì tải /song-builder?standalone=1).

- **PHASE 2 + 3 web XONG + verify trình duyệt (2026-06-15, tsc sạch, CHƯA commit/push):** **Phase 2** `src/PracticePlayer.tsx` (MỚI) — Player luyện tập web tự chứa: iframe YouTube + đồng hồ media (postMessage/videoClock) + metronome Web Audio + karaoke tự cuộn (scrollIntoView block center, mask gradient mép, lưu ý dùng `padding:'8vh 0'` KHÔNG dùng % vì %padding tính theo width) + hợp âm trên từ + dải hợp âm hiện tại có ChordDiagram + stats. Nối vào SongBuilderPage: state practiceDraft, DraftsModal thêm nút "▶ Luyện tập" (onPractice). **Phase 3** — Xuất/Nhập .bms: `serializeDraft`/`importDraftJSON` trong songDraftStorage.ts (import cấp id mới); DraftsModal thêm "⤵ Nhập bài (.bms)" (file input) + nút ⤴ chia sẻ mỗi dòng (Blob download <title>.bms) + ghi chú. Responsive desktop: hằng `MAXW=760` + style `center`, áp vào header 2 hàng + video bar + content scroll + footer → căn giữa cột trên màn lớn. TOÀN BỘ 3 phase web HOÀN TẤT. CHƯA commit Phase 2+3 — chờ Văn Anh duyệt push.

**Vỏ Capacitor cũ (BỎ/treo):** `~/Desktop/beat-my-songs/` (appId `com.vananhaudio.beatmysongs`, `server.url=.../song-builder?standalone=1`). Vẫn còn trên đĩa nhưng đã bị thay bằng hướng native. Param `?standalone=1` trong [[song-builder-v1]] AppRouter (ẩn nút ✕) vẫn còn dùng được cho web.

**Why:** Văn Anh muốn đưa BMS lên Apple SỚM như sản phẩm độc lập, và muốn app NATIVE (không webview mỏng) để tránh rủi ro **Guideline 4.2** (vỏ webview dễ bị từ chối khi xét App Store công khai; TestFlight thì OK). "sau này sẽ tích hợp lại vào TVA".

**How to apply:**
- Bước Apple (chỉ Văn Anh làm): tạo app mới trong App Store Connect (bundle `com.vananhaudio.BMS`) → Archive/Upload từ Xcode. Tên "Beat my Songs" phải unique trên App Store.
- KHÔNG commit/push vào git project BMS nếu Văn Anh chưa yêu cầu rõ.
- (Hướng Capacitor cũ) pod install cần `LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8` — chỉ liên quan nếu quay lại Capacitor.
- App TVA hiện tạm mang icon BMS (chưa trả về) — Văn Anh nói cứ để, đang test.
