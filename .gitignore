# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Biến môi trường (chứa khoá API — KHÔNG commit)
.env
.env.*
!.env.example

# Claude Code — cấu hình quyền cá nhân (không commit)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Service account key — TUYỆT ĐỐI không commit
android/*.json
!android/capacitor.settings.json

# Artifacts nghiệm thu MusicXML — do harness/script trong tests/ sinh ra, TÁI TẠO ĐƯỢC.
# Không commit (xem lệnh tái tạo trong docs/musicxml-*/README.md). Không dùng Git LFS.
docs/musicxml-*/output/
*.musicxml-acceptance.zip
musicxml-*-acceptance.zip

# Ngoại lệ DUY NHẤT: 3 ảnh chụp UI Giai đoạn 3 — chụp tay, KHÔNG có script sinh lại,
# nên phải nằm trong repo. Vẫn chặn mọi thứ khác trong thư mục đó
# (PDF, SVG, PNG do harness sinh, ZIP, .source.musicxml).
!docs/musicxml-beats-mvp/output/
docs/musicxml-beats-mvp/output/*
!docs/musicxml-beats-mvp/output/ui-*.png

# Python bytecode của các verifier artifact
__pycache__/
*.py[cod]
